language: python
python:
  - "3.8"

install:
  - "apt install build-essential gcc odbcinst unixodbc unixodbc-dev"
  - "pip install --upgrade pip"
  - "pip install -r ./requirements.txt"

script:
  - "python manage.py test"

notifications:
  email:
    recipients:
      - royopa@gmail.com
    on_success: change # default: change
    on_failure: change # default: always
